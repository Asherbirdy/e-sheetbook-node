import { Router } from 'express'
import { SurveyController } from '../controllers'
// import { authenticateUser, checkVerifiedEmail } from '../middleware'
const router = Router()

// router.get(
//   '/showMe',
//   authenticateUser,
//   SurveyController.showCurrentUser
// )

export default router
